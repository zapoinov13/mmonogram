/**
 * Переключатели разделов сайта.
 *
 * Раздел, который временно не показываем, не вырезается из кода: страница,
 * её переводы и SEO остаются на месте, а сюда ставится false. Вернуть раздел —
 * поменять одно значение, а не собирать удалённое обратно по кускам.
 *
 * Файл намеренно без единого импорта: его читает и приложение через алиас
 * «@/lib/features», и каталог SEO, который загружается конфигом сборки, где
 * алиасов нет.
 */

/**
 * 3D-конфигуратор: страница /configurator и баннер на главной.
 *
 * Выключен с 22 сентября 2026 по просьбе владельца — раздел пока не
 * показываем. Со значением false:
 *   - баннер с главной пропадает;
 *   - маршрут /configurator не регистрируется, адрес отдаёт страницу 404;
 *   - страница уходит из sitemap.xml и получает noindex.
 *
 * Код конфигуратора, модели в public/models и переводы не тронуты.
 */
export const CONFIGURATOR_ENABLED = false;
